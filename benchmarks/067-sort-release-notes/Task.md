# Data Transformation: Sort Release Notes

Difficulty: easy

Transform the data in data/notes.txt into reports/notes.txt. Add a small script if useful, but the final report file must contain the transformed data deterministically.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.

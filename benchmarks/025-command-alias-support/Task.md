# Multi-file Application Change: Command Alias Support

Difficulty: easy

Update the small application to support command aliases. This requires reading the entry point, the helper module, and the test. Keep the existing run(input) export working.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.

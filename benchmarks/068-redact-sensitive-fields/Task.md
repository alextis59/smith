# Data Transformation: Redact Sensitive Fields

Difficulty: easy

Transform the data in data/users.json into reports/users.json. Add a small script if useful, but the final report file must contain the transformed data deterministically.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.

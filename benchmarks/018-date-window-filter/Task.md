# Single-file JavaScript Fix: Date Window Filter

Difficulty: medium

Fix the bug in src/filter-window.js. Keep the public function name unchanged and make the included test file pass with Node.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.

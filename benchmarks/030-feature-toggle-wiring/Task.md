# Multi-file Application Change: Feature Toggle Wiring

Difficulty: medium

Update the small application to support feature toggles. This requires reading the entry point, the helper module, and the test. Keep the existing run(input) export working.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.

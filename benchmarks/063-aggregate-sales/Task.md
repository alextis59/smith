# Data Transformation: Aggregate Sales

Difficulty: medium

Transform the data in data/sales.csv into reports/sales-total.txt. Add a small script if useful, but the final report file must contain the transformed data deterministically.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.

# Coherent Multi-step Task: Config Loader Validation

Difficulty: hard

Complete the loader utility. Merge config defaults, project overrides, and validation errors. This is intentionally multi-step: inspect all files, update implementation and docs, then run the test script.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.

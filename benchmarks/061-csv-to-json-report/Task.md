# Data Transformation: Csv To Json Report

Difficulty: easy

Transform the data in data/customers.csv into reports/customers.json. Add a small script if useful, but the final report file must contain the transformed data deterministically.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.

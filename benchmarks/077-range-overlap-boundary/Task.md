# Edge-case Handling: Range Overlap Boundary

Difficulty: easy

Fix the edge case in src/range.js without changing the exported function name. The included test describes the behavior expected by callers.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.

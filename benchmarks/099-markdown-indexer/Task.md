# Coherent Multi-step Task: Markdown Indexer

Difficulty: hard

Complete the indexer utility. Index Markdown headings with duplicate anchor suffixes. This is intentionally multi-step: inspect all files, update implementation and docs, then run the test script.

Work inside the provided workspace. Keep the task self-contained and do not use network access, secrets, package installs, or privileged commands.
